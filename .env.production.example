# Production env vars — set these in the Netlify dashboard under
# Site settings → Environment variables. DO NOT commit a .env.production
# file with real values.

# Full URL of the deployed backend API, including the /api path.
# Example: https://mer-tools-api.onrender.com/api
VITE_API_URL=
