# Backend API base URL.
# Leave unset in dev — Vite proxies /api → http://localhost:5000 automatically.
# In production, set this to the full backend URL.
# VITE_API_URL=https://api.mer-tools.example.com/api
